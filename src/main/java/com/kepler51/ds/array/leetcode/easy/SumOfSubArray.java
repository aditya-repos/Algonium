package com.kepler51.ds.array.leetcode.easy;

public class SumOfSubArray {

    private int addSubArray(int[] numbers){
        if (numbers.length == 1)
            return numbers[0];

        return -1;
    }
}
